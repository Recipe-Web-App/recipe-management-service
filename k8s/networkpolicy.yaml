---
# k8s/networkpolicy.yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: recipe-management-service-netpol
  namespace: recipe-management
  labels:
    app: recipe-management-service
spec:
  podSelector:
    matchLabels:
      app: recipe-management-service
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: recipe-management
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8080
  egress:
    # Allow DNS resolution
    - to: []
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # Allow PostgreSQL database connection
    - to:
        - namespaceSelector:
            matchLabels:
              name: recipe-management
        - podSelector:
            matchLabels:
              app: postgresql
      ports:
        - protocol: TCP
          port: 5432
    # Allow HTTPS for external API calls (if needed)
    - to: []
      ports:
        - protocol: TCP
          port: 443
