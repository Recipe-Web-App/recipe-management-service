---
# k8s/poddisruptionbudget.yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: recipe-management-service-pdb
  namespace: recipe-management
  labels:
    app: recipe-management-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: recipe-management-service
