---
# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: recipe-management-service
  namespace: recipe-management
  labels:
    app: recipe-management-service
spec:
  selector:
    app: recipe-management-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
