-- Recipe Manager Service Database Schema
-- Initial migration placeholder
--
-- This is a placeholder migration file to establish the Flyway migration structure.
-- Replace this with your actual database schema migration when ready.
--
-- Migration naming convention: V{version}__{description}.sql
-- Example: V001__Create_recipes_table.sql
--
-- Note: Never modify existing migration files after they have been applied.
-- Always create new migration files for schema changes.

-- Placeholder comment - replace with actual schema creation
-- CREATE TABLE recipes (
--     id BIGSERIAL PRIMARY KEY,
--     name VARCHAR(255) NOT NULL,
--     description TEXT,
--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
--     updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );

-- This placeholder migration does nothing
SELECT 1;
